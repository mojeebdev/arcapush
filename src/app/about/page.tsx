import AboutPage from "@/components/AboutPage";

export const metadata = {
  title: "About · Arcapush",
  description: "The origin story of Arcapush — the default registry for vibe-coded products.",
};

export default function Page() {
  return <AboutPage />;
}